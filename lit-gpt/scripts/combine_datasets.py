"""
After downloading and processing the datasets into separate .npy files,
we can combine them into a joint directory that allows for fine tuning over a combined dataset.
"""

import os

def combine_datasets(
    **kwargs,
):
    """
    kwargs to specify which folders to use, and what proportion of each
    """

    pass